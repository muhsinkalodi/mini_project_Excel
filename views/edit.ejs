<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
      <h5 class="mb-5">edit your post</h5>
      <form action="/update/<%= post._id %>" method="post">
        <textarea
          placeholder="What's on your mind ?"
          class="w-1/3 p-3 bg-zinc-900 resize-none bg-trasparent border-2 outline-none border-zinc-800 rounded-md w-full sm:w-1/2 lg:w-1/3 ..."
          name="content"
        >
<%= post.content %></textarea
        >
        <input
          class="px-3 w-40 py-2 text-sm bg-yellow-500 text-black block rounded-md mt-2 w-full sm:w-40 lg:w-40 ..."
          type="submit"
          value="Update Post"
        />
      </form>
    </div>
  </body>
</html>
